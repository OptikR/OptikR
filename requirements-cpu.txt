# OptikR Requirements - CPU Only Version
# Install: pip install -r requirements.txt && pip install -r requirements-cpu.txt

# PyTorch CPU-only (smaller download, works on all systems)
--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# PaddlePaddle CPU-only
paddlepaddle>=2.5.0
